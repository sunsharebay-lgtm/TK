# 创意空间 · 项目规则

本文件是「创意空间」项目的最高规则，任何在本项目里工作的任务（包括新建对话、接手任务、运行工具、写代码、写文档）必须先阅读本条，并始终遵守下面的原则。

## 1. 项目身份

- 本项目叫**创意空间**（英文 Creative Space），**不再叫 "Claude Code 坦克大战"**，也不是单纯的坦克大战项目。
- 项目根目录为 `创意空间/`。
- 任何文案、文档、标题、说明里，一律使用「创意空间」作为项目名，不要再用旧名。
- 「创意空间」是一个收纳多个独立创意的地方：游戏、工具、实验、素材都可以作为一个创意放入其中。
- 层级关系：总项目 `/Users/sun/Desktop/AI`（集团）→ 创意空间（总经理/总协调）→ 各子项目（独立部门）。
- 子项目工作时保持干净隔离；完成或计划发布时打版本标签，由创意空间统一联动首页与部署。

## 2. 组织原则：一个创意一个文件夹

- **每个创意单独占用一个文件夹，文件夹名就是这个创意的名字。**
- 该创意产生的**所有文件**都要放进它自己的文件夹，包括：
  - 源码 / 页面（如 `tank-battle.html`、`three-kingdoms.html`）
  - 素材与资源
  - 文档 / 规格 / 计划 / 交接文档
  - 测试
  - 历史版本与归档
  - 其他任何与该创意相关的文件
- **每个创意项目都要有自己的 `更新记录.md` 和 `历史版本/`**：
  - `更新记录.md`：记录该创意的每一次版本/阶段变化、做了什么、解决了什么问题。
  - `历史版本/`：归档该创意确认版本时被替换下来的旧源码。
  - 有更新时，在对应项目的 `更新记录.md` 末尾追加，不重写历史；并把上一版源码归档到 `历史版本/`。
- 不要把不同创意的文件混放在同一个文件夹里，也不要散落到根目录。

## 3. 新创意 = 新文件夹

- **一旦开始一个新的创意，就先在项目根目录创建一个以该创意命名的文件夹，再把后续产生的所有文件放进这个文件夹。**
- 文件夹命名直接使用创意的名字，例如：`创意空间首页/`、`坦克大战/`、`吞食天地三国/`、`超级玛丽/`、`墨水屏小站/`、`粉丝资源/`。
- 文件夹需要能让人一眼看出这个创意是什么；优先用中文创意名，必要时可带上英文/代号作为补充，但文件夹名主体必须是创意的名字。
- 在某个创意里做功能迭代，就继续在那个创意自己的文件夹里进行，不另外开文件夹，也不放到根目录。
- 如果两个创意确实产生共享代码，把共享部分放在根目录的 `共享/` 或 `scripts/` 里，并写明归属；不要因此打散各创意自己的文件。

## 4. 现有创意目录

下面是当前已经按创意归档好的文件夹，工作时应尊重这套结构：

```text
创意空间/
├── 创意空间首页/        # 创意：创意空间首页（GitHub Pages 起始页 / 游戏中心）
│   ├── index.html          # 真正的首页源码（以后改首页改这里）
│   ├── game-catalog.json   # 游戏目录数据
│   ├── game-catalog.template.json
│   ├── scripts/            # 版本目录生成脚本
│   └── 测试/               # 首页/游戏中心测试
├── 坦克大战/          # 创意：坦克大战
│   ├── tank-battle.html  # 当前源码
│   ├── 坦克大战.html     # 旧中文入口兼容跳转
│   ├── 历史版本/         # 各版本源码 + 桌面封装旧版
│   └── 测试/             # 坦克大战专用测试
├── 吞食天地三国/        # 创意：吞食天地三国 RPG
│   ├── index.html          # 入口跳转页（直达网页复刻版）
│   ├── three-kingdoms.html # 早期 FC 风单文件版（历史保留）
│   ├── 素材/             # 参考素材（docx/rtf/APK/exe/gmdata 等）
│   ├── 网页复刻/         # 当前主推进的网页复刻（完整 MZ 兼容引擎）
│   ├── 文档/             # 规格/计划/交接文档
│   └── 测试/             # 三国专用测试
├── 超级玛丽/            # 创意：超级玛丽
│   ├── index.html
│   ├── src/              # 分片源码
│   ├── test/             # 测试
│   ├── build.sh
│   └── README.md
├── 墨水屏小站/          # 创意：墨水屏小站（Kindle 墨水屏信息屏）
│   ├── index.html
│   ├── 测试/             # 墨水屏小站冒烟测试
│   └── 历史版本/
└── 粉丝资源/            # 创意：粉丝资源（视频资料/模板/详情页）
    ├── index.html
    ├── resources.json
    ├── 资源/             # 长资料独立页
    ├── 测试/
    └── 历史版本/
```

`创意空间首页` 是一个独立项目，包含线上入口页面、游戏目录数据、生成脚本与专属测试。**以后所有关于主页的修改，一律放在 `创意空间首页/` 文件夹里**。

**每个子项目都是平级独立项目**，在 Codex 中可单独注册为本地项目；它们统一归属于创意空间这一个 Git 仓库和 GitHub Pages 站点。

## 5. 根目录保留的共享内容

根目录只保留「创意空间」这个舞台本身需要的共享设施，不存放具体创意的业务文件：

- `AGENTS.md`：本规则文件。
- `README.md`、`文件夹说明.md`：项目说明。
- `更新记录.md`：**创意空间本地总更新记录**，专门记录整个项目每一次变化（含各分支项目）；它**仅保存在本地，不推送到 GitHub**，已在 `.gitignore` 与 Git 索引中排除，不要把它当普通项目文档提交。
- `index.html`：根部署跳转页，点击后自动跳到 `创意空间首页/`；它只是部署占位，**不要把它当首页修改**。
- `.github/`：GitHub Actions 部署工作流。
- `docs/`：项目级共享文档（如整体进度 `PROGRESS-SUMMARY.md`、项目状态台账 `项目状态.md`）。
- `项目关系与GitHub推送.md`：记录各项目关系与 GitHub 推送/发版方法，随时维护。
- 各创意的测试一律放进对应创意文件夹的 `测试/`。

## 6. 更新记录与历史版本

**哪个项目有变化，就记录在哪个项目里。**

- 每个创意项目（创意空间首页 / 坦克大战 / 吞食天地三国 / 超级玛丽 / 墨水屏小站 / 粉丝资源）都有自己专属的 `更新记录.md` 和 `历史版本/`，这两项属于仓库内容，会随 Git 一起推送 GitHub。
- 某个项目有新版本或阶段变化时：
  - 在**那个项目自己的** `更新记录.md` 末尾追加记录（不重写历史），写明做了什么、解决了什么问题。
  - 把被替换下来的上一版源码复制归档到**那个项目自己的** `历史版本/`。
- 任何一个分支项目发生变化，同时也要在根目录的本地总 `更新记录.md` 里补一条「整个创意空间发生了什么」。

**根目录的更新记录只存本地，不上传。**

- 根目录 `创意空间/更新记录.md` 是「创意空间本地总更新记录」，记录整个项目的每一次变化（含所有分支项目）。
- 它已在 `.gitignore`（`/更新记录.md`）中排除，并已从 Git 索引移除，**不会推送到 GitHub**。
- 不要把它当普通项目文档提交；各项目自己的 `更新记录.md` 不受这条限制，照常上传。

**开始任何工作前**：先确认本次变更属于哪个项目；完成后按本节规则补总记录与项目记录、归档历史版本。

## 7. 检查清单

开始或继续任何工作前，心里过一遍：

- [ ] 我是否确认了当前要处理的创意是哪一个？
- [ ] 这个创意的文件夹在根目录是否存在？不存在就新建并命名。
- [ ] 我要新增/修改的所有文件，是否都放在该创意的文件夹里？
- [ ] 有没有文件被错误地放到了根目录或其他创意的文件夹？
- [ ] `index.html` 等共享入口引用的路径，是否指向了正确的创意文件夹？
- [ ] 修改的是不是首页？是的话是否写在了 `创意空间首页/` 里（而不是根跳转页）？
- [ ] 项目关系或推送方式变化时，是否更新了 `项目关系与GitHub推送.md`？
- [ ] 某个创意有版本/阶段变化时，是否在它自己的 `更新记录.md` 末尾追加、并把旧源码归档到 `历史版本/`？
- [ ] 涉及整个创意空间的变化时，是否也补写了根目录的本地总 `更新记录.md`（且注意它不会被推送）？
- [ ] 结构变化、经验教训、环境变更时，是否同步更新了 `Codex重装交接指南.md`？
- [ ] 新增/改名/移除项目时，是否更新了 `Codex重装交接指南.md` 的目录速览与各项目章节？

## 8. 违反原则时的处理

- 发现文件放错位置，迁移到正确的创意文件夹，并同步更新所有引用路径。
- 不要在未告知用户的情况下，把某个创意的文件移到另一个创意的文件夹。
- 遇到「某一文件属于哪个创意」不清楚时，先读文件内容判断归属；仍不确定就询问用户，不要让规则模糊化。

## 9. 环境与恢复

- 环境与恢复手册（全项目总览）见 `~/Desktop/pgcs/00_交接总纲.md`。
- 重装 Codex 后先读该手册，再按 `~/Desktop/pgcs/Codex恢复备份/restore_codex.sh` 恢复。
- 本项目的 git 仓库、GitHub Pages 域名、版本标签规则见 `项目关系与GitHub推送.md`。

## 10. 维护重装交接文档

- [Codex重装交接指南.md](Codex重装交接指南.md) 是「重装 Codex 后无需额外说明即可继续工作」的项目级总文档，重装后先读它再工作。
- 结构变化、经验教训、环境变更时，同步更新该指南，并检查 `README.md` / `文件夹说明.md` / `项目关系与GitHub推送.md` 是否需同步。
- 新增/改名/移除项目时，更新指南的目录速览与各项目章节。
