# 创意空间 · 文件夹说明

> 「创意空间」按创意组织所有文件：每个创意一个文件夹，文件夹名就是创意名，该创意的所有文件都放在它自己的文件夹里。顶层规则见 [AGENTS.md](AGENTS.md)。

## 顶层目录结构

```text
创意空间/
├── AGENTS.md                  # 项目规则（最高规则，任何工作前必读）
├── README.md                  # 项目总览
├── 文件夹说明.md              # 本说明文档
├── 更新记录.md                # 创意空间本地总更新记录（仅本地，不推 GitHub）
├── 项目关系与GitHub推送.md     # 各项目关系 + GitHub 推送/发版方法
├── Codex重装交接指南.md          # Codex 重装后恢复工作的项目级总文档（随时维护）
├── index.html                 # 根部署跳转页（自动跳到创意空间首页，勿当首页改）
├── .github/                   # GitHub Actions 部署工作流
├── docs/                      # 项目级共享文档
├── 创意空间首页/              # 创意：创意空间首页（游戏中心 / 起始页）
├── 坦克大战/                  # 创意：坦克大战
├── 吞食天地三国/              # 创意：吞食天地三国 RPG
├── 超级玛丽/                  # 创意：超级玛丽
├── 墨水屏小站/                # 创意：墨水屏小站（Kindle 墨水屏信息屏）
└── 粉丝资源/                  # 创意：粉丝资源（视频资料/模板/详情页）
```

## 各创意文件夹

### 创意空间首页/

- `index.html`：真正的首页 / 游戏中心，通过卡片链接到各创意文件夹里的游戏页面。**以后所有主页改动都改这里。**
- `game-catalog.json`：游戏目录数据，供首页动态读取版本号。
- `game-catalog.template.json`：Pages 部署时生成游戏目录的模板。
- 粉丝资源区块：首页读取 `../粉丝资源/resources.json` 展示最新条目，详情页拼接 `../粉丝资源/` 前缀；内容由粉丝资源项目独立维护。
- `scripts/generate-game-catalog.cjs`：读取 Git 稳定标签，按 `game/<game-id>/vX.Y.Z` namespace 生成 `game-catalog.json`。
- `更新记录.md`：本项目专属更新记录。
- `project.md`：本项目交付规格、处理决策与 QA 记录。
- `历史版本/`：本项目已确认版本的旧首页源码归档。
- `测试/`：首页/游戏中心专属测试（`game_center_test.js`、`version_catalog_test.js`）。

### 坦克大战/

- `tank-battle.html`：坦克大战当前稳定版 `v1.6.1` 游戏源码（单文件 Canvas，无外部依赖），后续迭代的主要修改对象。
- `坦克大战.html`：旧中文入口兼容跳转页，访问后跳转到 `tank-battle.html`。
- `更新记录.md`：本项目专属更新记录（v1.0 至 v1.6.1）。
- `历史版本/`：已归档的旧版源码 `坦克大战-v1.0.html` 至 `v1.6.html`，以及已废弃的 Electron 桌面封装旧版。
- `测试/`：坦克大战专用测试与诊断脚本。

### 吞食天地三国/

- `index.html`：入口跳转页，打开后直达当前主推进的网页复刻版。
- `three-kingdoms.html`：早期 FC 风格单文件版（历史迭代保留，已不再作为在线主入口）。
- `素材/`：参考素材，包括图文攻略/答题、安卓 APK、电脑端正式版（exe + gmdata 数据）。
- `网页复刻/`：当前主推进的网页复刻（原版数据 + 完整 MZ 兼容引擎），主入口为其中的 `吞食天地Ⅱ同人复刻-网页版/index.html`。
- `文档/`：规格、开发计划与交接文档。
- `更新记录.md`：本项目专属更新记录。
- `历史版本/`：本项目已确认版本的旧源码归档。
- `测试/`：三国专用测试（`three_kingdoms_smoke.js`、`three_kingdoms_test.js`）。

### 超级玛丽/

- `index.html`：像素平台跳跃游戏（构建产物）。
- `src/`：分片源码（角色、关卡、精灵、音频、渲染等）。
- `test/`：测试。
- `build.sh`：构建脚本。
- `README.md`：该创意自己的说明。
- `更新记录.md`：本项目专属更新记录。
- `历史版本/`：本项目已确认版本的旧源码归档。

### 墨水屏小站/

- `index.html`：墨水屏信息屏入口（翻页时钟、天气、番茄钟、阅读清单；ES5 兼容老 WebKit）。
- `README.md / AGENTS.md / project.md`：项目手册、边界规则与任务记录。
- `更新记录.md`：本项目专属更新记录。
- `历史版本/`：本项目已确认版本的旧源码归档。
- `测试/`：本项目冒烟测试（`smoke_test.js`）。

### 粉丝资源/

- `index.html`：粉丝资源独立入口页，按编号展示全部资源。
- `resources.json`：资源数据源（固定编号、标题、简介、详情页地址），由本项目手动维护。
- `资源/`：长资料独立页（当前：`giffgaff保命资料/`）。
- `README.md / AGENTS.md / project.md`：项目手册、边界规则与任务记录。
- `更新记录.md`：本项目专属更新记录。
- `历史版本/`：本项目已确认版本的旧资料页或入口源码归档。
- `测试/`：资源数据与详情页校验（`resources_test.js`）。

## 根目录共享设施

- `index.html`：根部署跳转页，打开后自动跳到 `创意空间首页/`；保持旧地址 `/TK/` 可用，只是占位，不要当首页修改。
- `.github/workflows/pages.yml`：`main` 分支或 `game/**/v*` 标签推送时自动部署 Pages。
- `docs/PROGRESS-SUMMARY.md`：整体进度总结（历史快照）。
- `docs/项目状态.md`：每个子项目当前状态台账，由定时汇报任务维护。
- `项目关系与GitHub推送.md`：各项目关系与推送方法，随时维护。

## 文件如何归类

判断一个文件该放哪里，按以下顺序：

1. 它属于哪个创意？放进那个创意的文件夹。
2. 不专属于任何创意、只服务创意空间本身？放在根目录共享设施。
3. 属于某个创意的测试 → 放进该创意文件夹的 `测试/`。
4. 属于某个创意的文档 → 放进该创意文件夹的 `文档/`。
5. 新创意 → 在根目录新建同名文件夹，所有产物都放进去。

## 更新记录

- 根目录 [更新记录.md](更新记录.md) 是**创意空间本地总更新记录**：记录整个项目每一次变化，**仅保存在本地，不推送到 GitHub**（已在 `.gitignore` 与 Git 索引中排除）。
- 每个分支项目（创意空间首页 / 坦克大战 / 吞食天地三国 / 超级玛丽 / 墨水屏小站 / 粉丝资源）各自有 `更新记录.md` 与 `历史版本/`，跟随仓库上传。
- 有新增更新时，在对应项目的 `更新记录.md` 末尾追加；同时在本机这份根目录总记录里补一条「整个创意空间发生了什么」。

## 当前版本

- 坦克大战：`v1.6.1`（2026-08-17）。
- 三国 RPG：第一稳定垂直切片 `v0.1.0`，开发线 `v0.2.0` 进行中。
- 超级玛丽：`v0.1.0` 开发版（原项目名“超级像素兄弟”）。
- 墨水屏小站：`v0.4.0` 开发版（2026-08-28 迁入）。
- 创意空间首页：`v0.5.0`（板块导航与子项目拆分）。
- 粉丝资源：`v0.1.0`（独立子项目拆出）。
