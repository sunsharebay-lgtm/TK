# 坦克大战 · 文件夹说明

> 本文档介绍 `坦克大战` 文件夹内每个文件与文件夹的用途，以及新版本的归档流程。

## 目录结构

```text
坦克大战/
├── index.html                  # 小游戏中心入口
├── game-catalog.json           # 当前稳定版本生成的游戏目录
├── game-catalog.template.json  # 游戏目录生成模板
├── tank-battle.html            # 当前最新版本 v1.6.1 游戏源码（单文件）
├── 坦克大战.html               # 旧中文入口兼容跳转页
├── scripts/                    # Pages 部署辅助脚本
│   └── generate-game-catalog.cjs# 根据 Git 稳定标签生成游戏目录
├── 更新记录.md                # 逐版本更新文档
├── 文件夹说明.md              # 本说明文档
├── 历史版本/                  # 已确认的旧版本源码归档
│   ├── 坦克大战-v1.0.html    # v1.0 早期原始版本
│   ├── 坦克大战-v1.1.html    # v1.1 地图/AI/跨关/口令改进版
│   ├── 坦克大战-v1.2.html    # v1.2 出生脱困/中文界面/高清渲染版
│   ├── 坦克大战-v1.3.html    # v1.3 移动端超级按键/寻路拥堵修复版
│   ├── 坦克大战-v1.4.html    # v1.4 周目/地狱绝境/道具与耐久修订版
│   └── 坦克大战-v1.5.html    # v1.5 周目成就/挑战计时/超级按键标记版
└── 历史版本/桌面封装-旧版/    # 已废弃的 Electron 桌面封装源码
    └── desktop/
        ├── app/index.html     # 桌面版内置的游戏副本
        ├── main.cjs           # Electron 主进程
        ├── preload.cjs        # 预加载脚本
        ├── package.json       # 桌面版配置与打包命令
        ├── pnpm-lock.yaml     # 依赖锁文件
        ├── pnpm-workspace.yaml# pnpm 工作区配置
        ├── scripts/sync-html.cjs# 源码同步脚本
        ├── .github/           # GitHub Actions 构建工作流
        └── README.md          # 桌面版构建说明
```

## 文件与文件夹用途

### index.html

- 原有 Pages 根地址的入口页面，现在展示小游戏中心。
- 通过游戏卡片链接到各个独立游戏页面。

### tank-battle.html

- 当前最新版本的坦克大战游戏源码，直接打开即可游玩。
- 单文件 Canvas 游戏，无外部依赖。
- 这是坦克大战后续迭代的主要修改对象。

### 坦克大战.html

- 旧中文游戏入口的兼容跳转页。
- 访问后自动跳转到 `tank-battle.html`，避免旧链接失效。

### game-catalog.json / game-catalog.template.json

- `game-catalog.json` 保存当前稳定版本对应的游戏目录，供小游戏中心读取。
- `game-catalog.template.json` 是 Pages 部署时的生成模板，版本号由 Git 标签填入。
- 不要手动把首页版本号当作长期维护项；发布新稳定版时创建 `vX.Y.Z` 标签即可。

### scripts/generate-game-catalog.cjs

- 接收 Git 稳定版本标签，例如 `v1.6.2`。
- 根据模板生成 `game-catalog.json`。
- GitHub Pages 工作流会在上传站点前自动执行这个脚本。

### 更新记录.md

- 记录每一个已确认版本的改动内容、改进方向与解决的问题。
- 每个版本记录包含日期、时间、文件位置和具体变更。
- 有新版本时直接在文件末尾追加，不重写历史内容。

### 文件夹说明.md

- 当前这份文档，用于说明目录结构、每个文件/文件夹的用途。
- 目录结构发生变化时同步更新。

### 历史版本/

- 存放已经确认的旧版本源码。
- 只保留源码文件，不存放构建产物、安装包或其他资源。
- 命名规则：`坦克大战-vX.Y.html`。

### 历史版本/桌面封装-旧版/

- 这里保存已经停止使用的 Electron 桌面版封装源码，不属于当前正式运行形态。
- `desktop/app/index.html` 是桌面版曾经使用的游戏副本。
- `desktop/scripts/sync-html.cjs` 曾经负责从根目录源码同步游戏文件。
- `desktop/package.json` 保存桌面版启动与打包命令：
  - `pnpm start`：同步源码并启动桌面版。
  - `pnpm build:mac` / `pnpm build:win`：生成安装包。

### 当前正式运行形态

- 当前采用网页版，不再追求桌面安装包。
- Pages 根地址 `/TK/` 是小游戏中心，旧二维码仍可使用。
- 正式坦克大战源码是根目录的 `tank-battle.html`。
- `坦克大战.html` 仅作为旧中文入口的兼容跳转页。
- 桌面封装仅保留在历史目录中，方便查阅，不再作为当前发布入口。

## 新版本归档流程

1. 在根目录 `tank-battle.html` 上继续迭代开发。
2. 取得阶段性成果后，向用户确认是否定为一个新版本。
3. 用户同意后：
   - 把即将被替换的上一版源码按原版本号归档到 `历史版本/坦克大战-vX.Y.html`。
   - 根目录 `tank-battle.html` 只保留新确认的最新版。
   - `坦克大战.html` 保持为兼容跳转页，不放置游戏源码。
   - 在 `更新记录.md` 末尾追加新版本记录（日期、时间、改动、解决的问题）。
   - 桌面封装不再同步，继续作为历史资料保留。
4. 用户未确认前，不算新版本，不归档、不记录。
5. 发布正式版本时创建并推送对应的 `vX.Y.Z` Git 标签，Pages 工作流会自动把最新稳定标签写入游戏目录。

## 当前版本

- **v1.6.1**（2026-08-17）
- 根目录为当前 v1.6.1 源码，文件为 `tank-battle.html`，v1.5 已归档到历史版本。
- 更新记录见 [更新记录.md](./更新记录.md)。
