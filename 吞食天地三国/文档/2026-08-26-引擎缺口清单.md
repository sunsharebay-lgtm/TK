# 网页复刻引擎 · 第一章验收与缺口清单（2026-08-26）

> 接续《2026-08-26-接手验证与CDP驾驶记录.md》。本轮完成：战斗系统实机验证、技能类型/装备槽数据核查、第一章关键战斗节点触发，并定位到首条确认缺口。

## 一、实机验证证据（headless Chrome + CDP，无人工试玩）

1. **启动与渲染**：`?autostart&autointro=18` 自动完成 难度选择 → 开场 → 皇宫收朱灵 → 徐州收关羽/张飞；全程零控制台错误、零未捕获异常（仅 favicon 404 噪音）。
2. **渲染疑虑排除**：canvas 816×624，像素统计 opacity=100%、平均亮度 141——此前开局截图"场景黑屏"确认是加载期现象，非渲染缺陷。
3. **战斗引擎可推进**：注入 `T.PendingBattle = {troopId:16,...}` 触发纪灵战（地图23 @115,141 的剧情战斗事件，页码条件未满足时不可直接触发，故用引擎原生入口注入）：
   - Scene_Battle 正常进入，phase: actor-cmd → select-skill → select-target 流转；
   - 30 次按键实机推进到**第 10 回合**：我方普攻 43 点损伤、敌兵反击、战斗日志正常；
   - 全程零异常；队伍 4/4 存活，属性完整（刘备 Lv1 216/216、关羽 241、张飞 221、朱灵 186）。
4. **战斗指令窗源码确认**：`cmds = ["攻击","计策","兵法","阵型","奥义","防御","道具","逃跑"]`，指令可用性按 `usableSkills(stypeId)` 过滤。

## 二、确认缺口

- **G1【已修复 · 2026-08-26 提交 37abf2c】歌唱战斗指令**：`js/battle.js` 指令列表在「阵型」与「奥义」之间补入「歌唱」（`cmds = [攻击,计策,兵法,阵型,歌唱,奥义,防御,道具,总攻,情报,逃跑]`），技能选择/施放完全走既有通用管线。
  - 实测证据：战斗中 `cmds` 含歌唱；授予歌唱技的武将 avail 列表含歌唱；直接行动注入后，佳人歌（scope2 全敌）正常施放——日志「纪灵/梁刚/陈纪/兵士 陷入了异常状态！」，刘备扣 20MP，全程零异常；
  - 与并发提交的「总攻/情报」指令共存（commit a53853a 之后之上）；
  - 回归：常规战斗推进到第 6 回合、敌兵阵亡 1 名、我方无损、零异常。

- **G1【确认】歌唱技能无法在战斗中使用**：`$dataSystem.skillTypes = ["","计策","兵法","阵型","歌唱","奥义"]`；技能数据 stypeId 分布 = 计策108 / 兵法18 / 阵型24 / **歌唱45** / 奥义1。战斗指令列表**没有"歌唱"**，`battle.js` 全文无"歌唱"处理——45 首歌唱技能（长河吟、蒿里曲、薔露歌、垓下歌……）在战斗中无入口。修复方向：`cmds` 增加"歌唱"，复用 select-skill 流程，补 singing 状态结算（如有）。
- **G2【待实玩】阵型/兵法/奥义的实际效果**：指令存在、数据存在，但效果链路（阵型 buff、兵法技、奥义消耗）未逐条验证。
- **G3【待核对】8 槽装备**：`equipTypes = ["","武器","盾牌","头部","身体","坐骑","副将","饰品","饰品2"]` 与 HANDOFF 所述一致；`Scene_Equip` 目前只显示武器槽与已装备槽（menus.js `if (i === 1 || this.actor.equipAt(i))`），坐骑/副将/饰品能否正常穿戴与生效需实装核对。
- **G4【建议】输入手感**：主循环 setTimeout(16ms)，快速点按偶有漏帧（HANDOFF 已知）；建议改 requestAnimationFrame。
- **G5【进行中】第一章剧情全流程**：本轮到"战斗节点注入"层面；从徐州出发的野外行走→山洞→寿春→剧情条件链（开关/变量）尚未逐步通关验证。

## 二、确认缺口

- **G2【已修复 · 2026-08-26】阵型/兵法/奥义实装**：
  - 阵型：新增 T.FORMATIONS（12 阵型参数表，暂定初版数值待校准）+ Game_Party.setFormation/formation/formationName + Game_Actor.formationBonus 并入 param 结算；阵型公共事件（101-112，脚本 $gameParty.setFormation(N)）经战斗公共事件执行器生效，实测鹤翼阵：攻击 15→30、防御 20→35、日志「摆出了鹤翼阵！」、零异常；
  - 兵法/奥义：与歌唱同走通用技能管线（选技→目标→applyItem），奥义「张凌剑」实测对敌造成 37-41 损伤、零异常；
  - **公共事件执行器**（battle.js runBattleCommonEvent）：支持 111 脚本分支/117 嵌套调用/355 脚本/101+401+405 消息/121 开关/122 变量/249 音效/201 撤离传送；修复 401/405 文本参数位；撤离经 _EscapeGuard 在行动序列中生效（修复动画内联清队列绕过 afterAction 的问题）；
  - **MZ note 元数据提取**（DataManager.extractMetadata / extractMetadataAll）：此前 $dataMap.meta 恒为空 → 所有依赖 <SuoDiOk>/<TroopVariables> 等标签的脚本全部失效；现已对全部数据集与地图生效（实测 map30/31 meta.SuoDiOk=true、刘备 meta.TalentId=102）；
  - 缩地计全链路实测：洞穴（SuoDiOk=true）战斗中施放 → 撤离战斗 → 传送地图1(9,10)，零异常；无 SuoDiOk 时显示「使用了缩地计，但是什么也没发生。」

## 二、确认缺口

- **G3【已修复 · 2026-08-26】八槽装备**：
  - 缺口：Scene_Equip.rebuildSlots 只显示武器槽+已装备槽 → 空槽位永远无法装备第一件坐骑/副将/饰品；
  - 修复：装备界面显示全部 8 槽（武器/盾/头/身/坐骑/副将/饰品/饰品2）；候选列表按 armor.etypeId 过滤（数据实测：盾71/头11/身11/坐骑10/副将33/饰品69，饰品2 暂无数据）；
  - 实测：坐骑槽候选=白马、副将槽候选=公孙瓒；穿戴白马(50)后速度 185→190（params[6]=5 生效）、副将公孙瓒(122)入槽，全流程零异常；
  - 附带修复：index.html 8 个 js 引入加 ?v=20260826 缓存破除——实测发现浏览器 HTTP 磁盘缓存按 URL 精确命中，代码更新后老玩家可能长期跑到旧文件（此前菜单/战斗改动就是因此时灵时不灵）。

## 二、确认缺口

- **G5 第一轮【进行中→静态覆盖已闭环】事件指令覆盖审计**：
  - 全量 566 张地图 + 公共事件脚本做指令码普查：引擎已处理约 130 个指令码，全数据使用中**唯一缺失的指令码 = 136**（9 处，护身烟/烟遁计/强身烟链路）；
  - 已实现：事件指令 136（遇敌禁止开关 0=禁止/1=开放，同步 switch 38）+ checkEncounter 尊重 switch38 + 菜单道具 effect 44（公共事件）走 T.runMapCommonEvent 迷你执行器（支持 0/101-102/111/117/121/122/124/125-128/136/249/355/357/401-405/411-413/505；选择默认第一项）；
  - 实测（A/B 对照组，零异常）：护身烟使用 → switch38=ON 且消息「队伍被奇怪的烟雾包围了。」；强身烟 → 开关关闭+两条提示；无保护时山洞遭遇正常进战（候选 11 组 → Scene_Battle）；switch38=ON 时同一触发被闸门拦截（Scene_Map，pending=N）；
  - 说明：行走类按键驱动在 headless 下未能触发玩家移动（疑似引擎移动采样与 CDP 按键时序的兼容问题，已纪录在案），第一章剧情全流程的“真实行走触发”部分待真机/浏览器人工试玩或改用定位式驱动继续（下一轮）。

## 二、确认缺口

- **G5 第二轮【进行中】第一章剧情链路实测**：
  - 剧情驱动图（已还原）：var1 递增闭环——开局+5 → 关羽加入+10 → 张飞加入+25 → 纪灵战(门≥25)后+30 → 袁术本阵(门≥30)后+35 → EV3@(130,97)战后+40 → 寿春山洞总攻(门≥40)后+45；高升孙仲门=var20≥5（由寿春城民居二 map168 EV3 +5 开启）；
  - 无头走查器（测试/tools 已随驱动使用）：实际完成 收朱灵/路昭+关羽+张飞 → 宫殿→徐州→大地图 全自动；发现走查器与战斗收尾存在竞态（战场未结束就传送走），已记录优化方向：每幕等战斗结束后再进下一幕；
  - **确认并修复恶性 bug：物理/谋略伤害无下限**——Lv1 队伍对高防敌人（纪灵军/陈纪）伤害恒为 0，战斗无限循环无法推进；修复为至少 1 点（与原版 max(1, atk*4-def*2) 及 A 线规格一致）；
  - 实测：修复后出现 1~39 点伤害（无 0 伤害行）；纪灵战全流程胜利 → 获得 193 金 → var1 25→30 → 战后回地图，零异常——**战后事件续跑链路确认正常**；

## 三、路线扫描结论（552 张地图全量）

- 纪灵战 troop16：地图23 大地图 @(115,141) 触发0（行动按钮）
- 袁术本阵 troop17：地图23 @(156,92) 触发1（接触）
- 袁术总攻 troop19：地图31 寿春山洞 @(21,6) 触发0
- 高升/孙仲 troop20：地图31 寿春山洞 @(30,21) 触发0
- 关键地名匹配 122 张地图；工具输出 `/tmp/ch1-route.json`（扫描脚本 `测试/tools/scan-chapter1.mjs`）

## 四、工具沉淀

- `测试/tools/cdp-driver.mjs`：无头驾驶（导航/按键带 80ms 保持/定时截图/异常收集/页面求值/预求值重试），零第三方依赖。
- `测试/tools/scan-chapter1.mjs`：地图战斗/出口/遭遇扫描器。

## 五、下一步

1. G1 修复：战斗指令加"歌唱"并验证 45 首技能可选中、可结算（先写 CDP 断言脚本再改 battle.js）；
2. G2/G3 实装验证（阵型效果、坐骑/副将穿戴）；
3. G5 剧情全流程：按攻略从徐州出城行走→山洞→纪灵（真实触发，不注入）→寿春→两连战，收集事件指令缺口；
4. G4 输入手感改造（rAF）作为低风险优化，改造后重跑战斗回归。

---

### 实测运行数据备忘

- 启动服务：`cd 网页复刻/吞食天地Ⅱ同人复刻-网页版 && python3 -m http.server 8642`
- 无头驾驶示例：`node 测试/tools/cdp-driver.mjs --url "http://localhost:8642/?autostart&autointro=18&goto=23,114,141" --duration 30000 --keys "Enter:260:30" --pre-eval-file /tmp/pre-set.json --eval-file /tmp/end-full2.json`


## 六、仓库并行推进记录（重要）

- 2026-08-26 00:30-00:42 期间，权威仓库出现并行提交（Codex 会话）：
  - `7f983e9` fix: A 系列自动图块范围判定修复（即此前同步的 mapscene.js 修复，已被人家正式提交）
  - `42261d7` fix: 战斗界面布局调整（武将上移、回合/行动提示居中）
  - `a53853a` fix: 战斗回血/菜单/动作反馈，新增总攻与敌方情报；data.js xparam 默认值修复（避免无再生特性单位每回合回满血）
- **教训**：`python3 -m http.server 8642` 实际服务的是仓库路径（Codex 会话遗留进程），工作区快照落后于仓库（battle.js/data.js）；本轮已把仓库最新 battle/data/mapscene 同步回工作区快照。后续工作应以仓库为唯一事实源，改完双向同步。

## 七、G1 修复提交

`37abf2c fix: 战斗指令补歌唱，45首歌唱技可在战斗中选择施放`（1 file changed, +1/-1）


## 八、G5 第三轮：第一章「灭袁术」全流程无头自动走查完成（2026-08-26）

- 走查链路（页面内驱动器 + 长会话 keep/attach，零人工操作、零异常、零 console 警告）：
  - 开场 intro（var1=5）→ 曹操对话收朱灵/路昭 → 关羽里程碑（var1=15，EV8）→ 张飞里程碑（var1=25，EV12）→ 出宫殿 → 徐州东门 → 大地图；
  - 纪灵拦路战（var1=30）→ 袁术本阵（var1=35）→ 寿春城民居二（map168 开高升门 var20=5）→ 寿春山洞高升孙仲战 → 袁术总攻战（var1=45=章末里程碑）；
  - 终态：var1=45、金 1329、队伍 5 将齐全、零异常——第一章全流程可玩链路确认打通；
  - 走查器关键经验（已沉淀）：事件 205 移动路线会卡（先传送到事件格再触发）、战后必须等待战斗真正结束再传走、intro 期间勿抢跑、变量为绝对值 SET 语义（[id,op,opt,a,b]，值在 b）。
- G4 输入手感：主循环 setTimeout(16ms) → requestAnimationFrame（保留降级），实际跑测正常（Scene_Map、零异常）。
- 音效抽查：SE(Ok)/ME(Victory1)/BGM(Town_01) 播放无异常（SE 别名修复此前已完成）。
- **目标完成情况**：缺口清单 G1-G5 全项已修复并附实测证据；剩余后续事项（不属于本次缺失修复）：阵型 12 阵数值为暂定初版待对照原版校准；建议真机/浏览器人工试玩做最终体验验收。


## 九、8 槽装备完整数值核对（2026-08-26 复核）

- 全部 7 个有数据的槽位核对通过（CDP 电池，逐槽 换装/卸下/属性增量）：武器 铜剑换短剑 atk+10 ✓；盾/头/身 为初始同款（刘备开局即戴 木盾/头巾/布衣，属性一直生效，此前“0 差值”属正常）；坐骑 白马 agi+5 ✓；副将 高升 0 ✓；饰品 布制披风 def+10 ✓；
- 槽 8「饰品2」当前数据无任何物品（空槽位，正常）；
- 存档往返：坐骑/副将/饰品 穿戴状态 save→load 完全一致（actorSnapshots.equips 覆盖全部槽位）；
- 回归资产：`测试/tools/cases/equip-8slot.pre.json / equip-8slot.end.json`（配合 cdp-driver 可随时重跑）；结论：G3 无需代码改动，8 槽装备功能完整。


## 十、玩家实测反馈修复清单（2026-08-26 第二轮试玩）

1. 菜单只有刘备：状态/技能/装备场景全部支持 PgUp/PgDn 切换武将（状态、技能为本次新增；装备原有）；
2. 物品共享与容量：背包为全队共享（非每将独立），数量无硬上限；物品列表行高收紧（fontSize 20）一屏显示更多；
3. 赤心丹恢复 500 兵力：数据即参考版解包值（+500），与原版一致；银仙丹+2000/金仙丹+4500/智复丹+100 谋点同源；
4. 难度/敌强度：全部敌人数值来自参考版解包数据（与原版同一来源），未做过任何增强/削弱；此前修复的“伤害下限≥1”与原版公式一致，不打折难度；
5. 技能只有刘备：技能场景已支持成员切换；且菜单内可直接使用非战斗技能（赤心计等恢复计回血、阵型切换等），攻击敌方类在菜单中提示“只能在战斗中使用”；
6. 刘备/朱灵/路昭无头像：根因是菜单/状态从未预载头像图（faceCache 只在消息窗填充）→ 已修复：主菜单与各子场景打开时预载全员头像（资源文件本就齐全）；
7. 走路只显示一人：新增跟随显示——队伍>1 时最多显示 3 人（刘备+2 队友），≤3 人按实际人数；跟随者按足迹尾随、带朝向与步行动画；
8. 行走摇晃过快：步行动画帧率减半（0.5→0.26 基础 + 速度系数），晃动明显放缓；
9. 附带：无头驾驶工具方向键码修正（ArrowDown/ArrowRight…，此前以 Down/Right 发送导致引擎不识别——这就是一直无法无头行走的原因）。

验证（CDP 实测，全部零异常）：菜单→状态 PgDn 切换到关羽；物品选人使用赤心丹 100→216、数量-1；技能菜单使用赤心计回满 625 兵力、扣 10 谋点；行走 114→119 五格；跟随者 $关羽/$张飞 尾随到位；faceCache 全员预载成功。


## 十一、G2 第二轮：敌方阵型链路实装 + 阵型阵营隔离修复 + 技能消息 %2 修复（2026-08-26）

### 审计发现（静态 → CDP 确认）

1. **敌方阵型链路整体缺失**：
   - 敌阵型技能 142-153（【敌方】散开阵~八卦阵）→ effect 44 → 公共事件 121-132，脚本为 `$gameTroop.setFormation(N)`；
   - 但 `T.$gameTroop` 全引擎**只有 resetGameState 置 null，战斗中从不绑定**；`Game_Troop` 无 setFormation → 脚本抛 TypeError 被 evalCommonScript 的 try/catch 吞成 console.warn，敌方布阵零效果（静默失效）；
   - 数据普查：enemies.json 无任何敌人带 141-153 技能，现网数据流不可达，但补齐后未来敌军/敌群事件可用。
2. **阵型加成串阵营（真 bug）**：`Game_BattlerBase.formationBonus` 直接读 `T.$gameParty.formation()`——**我方摆阵时敌方同受加成**（如我方背水阵，敌方攻击+40）。FC 语义为阵型只作用于己方阵营。

### 修复内容

- `battle.js`：Game_Troop 新增 `setFormation(n)`（n=-1 无阵型/解阵语义，0..11 同 T.FORMATIONS）/`formation()`/`formationName()`；Scene_Battle 构造器绑定 `T.$gameTroop = this.troop`，finish() 解绑 null；
- `data.js`：基类 `formationBonus` 归零；`Game_Actor.formationBonus` 读队伍阵型、`Game_Enemy.formationBonus` 读敌群阵型（战场外 $gameTroop=null → 0），两侧隔离；
- `battle.js` runBattleCommonEvent：敌方阵型变化时日志「敌方摆出了X阵！」（与我方「摆出了X阵！」对称）；
- `battle.js` applyItem：message1 补替换 `%2`（此前只替换 %1，所有带 %2 的技消息显示「布下%2！」字面量；现「刘备布下散开阵！」）。

### 实测证据（headless Chrome + CDP，`formation.pre/end.json` 用例，全程零异常零 console 错误）

- `?autostart&autointro=18&goto=23,114,141` + PendingBattle troop16 注入（同此前后无头纪要做法）；
- `wired=true`：战斗期 `$gameTroop === BattleScene.troop`；
- 敌方鹤翼阵（CE 122）：`runBattleCommonEvent(122,en)` 后敌群 formationName=鹤翼阵，首敌 atk/def 增量 [15,15,0,0,0]，日志含「敌方摆出了鹤翼阵！」；
- **阵营隔离**：敌群阵型置空后我方摆背水阵（atk+40/def-40/mdf-20）→ 敌方五项增量全 0（修复前会 +40/-40/-20）；我方刘备实测 [40,-20,0,-20,0]（def 20→0 为 param 下限 0 截断，属正常：基础 5+装备 15=20，-40 后封底 0）；
- 我方鹤翼阵（CE 102）：日志「摆出了鹤翼阵！」、队伍阵型=鹤翼阵；
- `%2` 修复：applyItem 散开阵（skill 122）→ 日志「刘备布下散开阵！」（此前为「布下%2！」字面量）；
- 附带：cdp-driver 增加 `--no-sandbox`（本机沙箱环境 Chrome 无法初始化自身沙箱导致 CDP 起不来，破环境适配，不影响行为）。

### 回归资产

- `测试/tools/cases/formation.pre.json` / `formation.end.json` / `formation.mid.json`（配合 cdp-driver 随时可重跑）；
- 命令：`node 测试/tools/cdp-driver.mjs --url "http://localhost:8642/?autostart&autointro=18&goto=23,114,141" --duration 10000 --keys "Enter:300:6" --pre-eval-file 测试/tools/cases/formation.pre.json --eval-file 测试/tools/cases/formation.end.json`；
- index.html js 缓存版本号 `?v=20260826` → `?v=20260826-r2`（data/battle 已变更，防老玩家跑旧文件）。

### 仍待办

- **阵型 12 阵数值校准**：T.FORMATIONS 仍是暂定初版（flat ±N）。本地无原版数值来源（TNW 引擎脚本封装在 UPX 压缩的 exe / 原生 APK 内不可读；A 线未实现阵型；攻略 docx 为截图式无数值），需人工提供原版阵型效果参考（截图/数值表）后校准。另注意 FC 语义为乘算倍率，校准可能需把 flat 加法改为倍率挂接。
- 真机/浏览器人工试玩终验（无头已验逻辑链路）。


## 十二、阵型数值校准：flat 加法 → FC 乘算倍率（推定值，待实机比对）（2026-08-26）

### 决策

- 用户确认：按 FC《吞食天地2》公开机制推定数值（标「推定值待实机比对」，原版参考留待实机阶段核实）；
- **实现语义切换**：FC 阵形为乘算倍率（背水阵攻2倍/防减半，随属性缩放），flat ±N 无法表达 → `param()` 改为对最终属性（基础×成长×buff＋特性＋装备）整体乘算 `formationRate(i)`；`formationBonus` 更名 `formationRate`（基类恒 1.0，我方/敌方各自读阵营阵型，保持第十一节阵营隔离）；
- 数值：12 阵按公开机制整定 round 倍率（1.0=不变）：

| 阵型 | 攻击 | 防御 | 智力 | 抗智 | 速度 |
|---|---|---|---|---|---|
| 散开阵 | 1.0 | 1.0 | 1.0 | 1.0 | 1.30 |
| 鹤翼阵 | 1.25 | 1.25 | 1.0 | 1.0 | 1.0 |
| 冲方阵 | 1.50 | 0.85 | 1.0 | 1.0 | 1.10 |
| 白马阵 | 1.15 | 1.15 | 1.15 | 1.15 | 1.15 |
| 鱼鳞阵 | 1.0 | 1.50 | 1.0 | 1.0 | 0.90 |
| 锋箭阵 | 1.40 | 1.0 | 1.0 | 1.0 | 0.85 |
| 一字阵 | 0.90 | 1.50 | 1.0 | 1.40 | 0.80 |
| 两仪阵 | 1.0 | 1.0 | 1.50 | 1.40 | 1.0 |
| 雁行阵 | 0.90 | 1.0 | 1.0 | 1.0 | 1.80 |
| 背水阵 | 2.0 | 0.50 | 1.0 | 0.80 | 1.0 |
| 掎角阵 | 1.0 | 1.40 | 1.30 | 1.30 | 1.0 |
| 八卦阵 | 1.0 | 1.80 | 1.40 | 1.60 | 0.80 |

### 实测证据（CDP，同一用例，allOk=true，零异常零 console 错误）

- 敌方鹤翼阵：首敌 atk/def 增量 [8,9] = 原值精确 ×1.25（round），日志「敌方摆出了鹤翼阵！」；
- 我方背水阵：刘备 [30,10,200,152,185] = [15,20,200,190,185] 精确乘算（攻×2、防×0.5 —— **不再被属性下限截断**，比 flat 版更接近原版语义）、抗智×0.8；
- 阵营隔离回归：敌群阵型置空时敌五项增量全 0（我方摆阵不影响敌方）；
- 我方鹤翼阵 CE / 散开阵 applyItem（%2 消息「刘备布下散开阵！」）保持通过；
- 常规战斗照常推进（第 2 回合，普攻/反击日志正常）。

### 仍待办

- **推定倍率待原版实机比对**：与 PC 正式版/安卓版游戏中各阵的武将状态页读数核对后微调（真机阶段）；
- 真机/浏览器人工试玩终验。
