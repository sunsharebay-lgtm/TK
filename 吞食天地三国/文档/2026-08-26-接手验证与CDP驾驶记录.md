# 接手验证与 CDP 无头驾驶记录

> 记录时间：2026-08-26
> 会话：在「DeepSeek hermes/吞食天地」工作区接手吞食天地三国项目（快照拷贝；权威仓库在 `/Users/sun/Desktop/pgcs/创意空间`）。

## 一、已落地

1. **同步自动图块修复**：`网页复刻/吞食天地Ⅱ同人复刻-网页版/js/mapscene.js` 已把原仓库中未提交的 A1-A4 自动图块范围修复（`id < 8192`，修复前 `< TILE_A5(1536)` 恒不成立）同步到工作区拷贝；两处文件现已一致，`node --check` 通过。常量确认：TILE_A1=2048、TILE_A5=1536、A1-A4 = 2048..8191。
2. **A 线（three-kingdoms.html）回归**：`测试/three_kingdoms_test.js` 327 项断言全部通过；`three_kingdoms_smoke.js` 78 通过 0 失败。
3. **本地服务**：`网页复刻/吞食天地Ⅱ同人复刻-网页版` 由 `python3 -m http.server 8642` 提供（PID 18893；重启命令见 README）。index/core.js/assets/data 均 200。
4. **无头驾驶工具**：`测试/tools/cdp-driver.mjs`（Node 内置 WebSocket 直连 Chrome DevTools Protocol，无第三方依赖）：
   - 启动 headless Chrome（固定调试端口 9333、独立 profile）
   - 定时截图（Page.captureScreenshot）
   - 按序发送按键（Input.dispatchKeyEvent，支持 Enter/Z 等）
   - 收集 console 异常、未捕获异常、>=400 资源响应（只发现 favicon.ico 404，属正常噪音）
   - `--eval` / `--eval-file` 在页面内求值游戏状态
5. **引擎启动验证（CDP 实测）**：
   - `?autostart&autointro=18&goto=159,14,7` 运行 14s：无控制台错误、无未捕获异常，当前场景 Scene_Map；
   - 自动推进已完成：难度选择 → 开场 → 皇宫 → 收朱灵 → 徐州收关羽、张飞；
   - 实测队伍 = 刘备(2)、关羽(3)、张飞(4)、朱灵(5)；位置 map 159 (14,7)；$dataActors=61、$dataSystem 59 键全解析；
   - 注意：此时金币为 0（自动确认按键可能已消耗或 500 金剧情未走完，待人工路线核对）。
6. **攻略文本**：`素材/吞食天地/同人复刻攻略.docx` 已提取正文到 `/tmp/tk-guide.txt`（约 4.9KB，文档主体是截图，文字为要点式路线）。

## 二、第一章路线（依据攻略 + HANDOFF-TAKEOVER.md）

开局 500 金 → 曹操对话收朱灵、路昭 → 徐州收关羽、张飞 → 城内搜刮/商店 → 训练/购物自行选择 → 出城大地图（黄巾/山贼随机战）→ 山洞搜刮（红点）→ 提前存档 → 纪灵拦路战 → 高升/孙仲 → 袁术本阵 → 总攻 → 章末。Boss 战不可逃跑；掉落不满意可读档刷。

## 三、已确认的引擎缺口/待核项（继续接手的清单）

1. 阵型、歌唱（三阶歌唱）效果：battle.js 中无相关实现关键词，需实测数据流；
2. 副将/坐骑/饰品多槽位装备：Scene_Equip 按 `T.$dataSystem.equipTypes` 动态生成槽位，需逐个核对 8 槽（武器/盾/头/身/坐骑/副将/饰品×2）价格与属性生效；
3. 输入手感：主循环仍是 setTimeout(16ms)，可改 requestAnimationFrame（HANDOFF 建议项）；
4. BGM/ME/SE 端到端核对：SE 别名已修复，剩余 Boss/胜负/开箱等音效待实测；
5. 地图事件指令覆盖：interp.js 已实现 122 个 case（101/102/111-133/201/205/206/212-251/301-320/355/357/401-413/505/601-655 等），552 张图的边角指令需边玩边验；
6. A 线（three-kingdoms.html）若继续：第二章、存档 v2、平衡调整（入口已切到 B 线引擎，优先级低）。

## 四、不确定性（本轮未验证项）

- 视觉预算耗尽前仅确认了标题画面与开局画面渲染正常、地图场景有 HUD；autostart 早期截图曾出现场景黑屏（疑似加载期），后续 CDP 运行确认 Scene_Map 正常且无异常，**最终黑屏问题是否彻底消失待下一轮截图复核**；
- 第一章完整流程（山洞/纪灵/高升/袁术两连战）尚未端到端跑完；
- 阵型/歌唱/装备槽为静态审计结论，属实装效果待实测。

## 五、下一步

1. 用 cdp-driver 继续驾驶：出城 → 山洞 → 纪灵（troop16）→ 寿春（troop20）→ 袁术本阵/总攻，逐步记录事件指令覆盖缺口；
2. 对缺口项（阵型/歌唱/8 槽装备/输入/音效）逐一最小实现 + 验证；
3. 每次改动按创意空间规则：先失败测试（B 线可建 CDP 断言脚本）、更新 `更新记录.md`、需要时归档历史版本。
