# 通宵开发日志（2026-08-27 01:00 → 08:00）

> 用户指令：不停优化直到早上 8 点。本日志按单元记录每轮完成的工作、实测证据与提交号。
> 基线提交：`511d470`（敌方阵型/阵营隔离）、`50ea9ac`（阵型倍率）、`c718918`（脚本 API 覆盖/客栈）。
> 并行会话未提交改动（battle.js 战斗 UI、index.html 版本号）不在本日志范围，避免提交冲突。

## 单元 1 · 新字段持久化（存档 v2）【完成】

**改动**（data.js）：
- 存档升级 `version:1 → 2`：party 新增 `formation/surrendered/winPoint/pvpLevel/pvpCount`，actorSnapshots 新增 `talents/chengHao`；
- 读档恢复新字段；**v1 旧档完全兼容**（缺字段 → 默认值 -1/[]/0）。

**实测（CDP，`persist-core.pre/end.json`，零异常零报错）`allOk=true`**：
- v2 往返：阵型 4、收服敌将 77=有、胜点 3、PVP 等级 2/计数 1、天赋 [102]、称号 [7]，saveInfo.version=2；
- v1 兼容：手写 v1 档加载后 formation=-1、talents/chengHao=[ ]、surrendered=[]、winPoint=0。
- 提交：`cae588f`（存档 v2）

## 单元 2 · 地图事件指令覆盖审计（第一轮）【完成】

**普查**（词边界精确）：全量 JSON 命令码 vs 引擎 case 全覆盖核对。
- 真实事件/路线缺口：`42`（成长道具效）、`14`（路线相对位移）、`204`（事件位移）、`333/334`（敌群事件页，排入后续单元）、`354`（分支终止，验证为无参安全）、`657`（插件 357 的多行续行——引擎本就不执行插件，标注忽略）；
- 22/23/31/32/51/52/61/62/64 均为**特性码**（trait），由特性系统处理，非缺口。

**修复**：
- **效果码 42（永久成长）**：`Game_BattlerBase.growParam(pid,n)`（累加 `_paramBonus`，param() 已并入）；战斗 `applyEffect` 补实装并输出「能力提升了！」日志；菜单 `useItem` 补 case 42（此前石头类道具菜单里 Buzzer 不可用）；`_paramBonus` 并入存档（serializeActors/loadGame）。
- **路线码 14**（相对位移 [dx,dy]，含地图边界钳制）。
- **事件码 204**（事件位移 [eventId,x,y,...]，与 202 同语义的宽化形式，推定参数位）。

**实测（CDP，`grow-item.pre/end.json`，零异常）`allOk=true`**：
- 菜单使用蛇胆：数量 1→0、最大兵力 +5；
- 战斗 applyEffect 武力石：抗智(dataId5)+10、日志含「提升了」；
- 存档往返：`_paramBonus` JSON 全等恢复。
- 提交：`4c9be08`（成长道具/路线码14/事件码204）

## 单元 3 · 敌群战斗事件页（333/334）【完成】

**发现**：troop 事件页字段为 `conditions`（非 condition）；300 敌群全部带页（多页=空页 [0]）。**第一章敌群(16/17/19/20/15/18/21/22)零实质页**——实现事件评估对已可玩内容零风险。另发现敌群名 `<Bgm:N>` 标签（战斗 BGM 按敌群切换）——本地仅 Battle_04.ogg 一个战斗 BGM，Bgm:5..10 无对应音频资产，无法实装（已记录）。

**实现**（battle.js）：
- `runBattleCommonEvent` 抽出共享核心 `execBattleEventList(list,user)`（公共事件与敌群事件页共用）；
- 新增 `runTroopEvents()`：MZ 语义回合数/敌HP/武将HP/开关条件判定，每页执行一次，回合起触发（startRound 内），事件消灭全部敌人 → 直接胜利；
- 新增战斗命令码 331/332/333/334/337（敌出现/变身/消灭/恢复，索引 -1=全体）；修复战斗侧 122 按标准参数位 [start,end,op,opt,value/script] 运算（此前只取前两位）；
- 注意：333 消灭只用 `die()`（隐藏≠死亡：isDead=!hidden&&state1，hide 会让 isDead 变 false）。

**实测（CDP，`troop-events.pre/end.json`，零异常）`allOk=true`**——注入测试页到 troop16：
- 回合2 触发 333 消灭敌索引3：存活 5→4、页标记已执行；再次评估不再生效（once-only）；
- 334/331 出现+复活：存活恢复 5；122 变量页：var99=777（op/opt 全参数位生效）。
- 回归：阵型用例 `allOk=true`（startRound 改动无副作用）、常规战斗正常推进。
- 提交：`0ee3c84`（用例+日志；battle.js 实现部分随并行会话工作区共存未入账）

## 单元 4 · 菜单/装备/技能/存档逐项实测【完成】

- **菜单巡游**（两轮按键驱动完整遍历 物品→技能→装备→状态→存档→返回,覆盖菜单树真实输入路径）：**零异常、零 console 错误**（首轮带 autointro 顺带验证了战斗内菜单路径;autointro 自动确认与手键有竞态,已识别为测试方法学问题,不影响结论）。
- **存档 UI 导线验证**（`menu-tour.pre/end.json`,注入 Input `_down.ok` 原子触发 `Scene_Save.update()`）：Enter→`saveGame(0)` 落盘,版本=2,存档含 队伍 刘备/关羽 等 —— `allOk=true`,零异常。
- 附带修正认知：`Scene_SaveLoad` 基类未导出,导出的是 `Scene_Save/Scene_Load` 子类(测试用子类)。
- 提交：`b0b4aee`（菜单巡游/存档UI导线用例+日志）

## 单元 5 · 音效/演出链路端到端核对【完成】

- **音频资产全面核对**：代码引用的全部 SE/ME 名 vs `assets/audio/`（SE 165 个，TNW 风格命名——引擎 SE_ALIAS 表 18 条已覆盖绝大多数）。
- **发现并修复唯一缺口**：胜利 ME `Victory1`（battle.js processVictory 播放）**无文件、无别名 → 胜利音效静音**。
  - `core.js`：SE_ALIAS 增加 `Victory1→Tstd_Win`；别名应用扩展 `dir==="se"||dir==="me"`（此前仅 se）。
- **实测（CDP，`audio-check.pre/end.json`，零异常）`allOk=true`**：16 个标准引用名全部经别名解析并成功加载缓冲（含 `me/Tstd_Win` 命中），无缺失音频。
- BGM 侧：`<Bgm:N>` 敌群标签（N=5..10）本地无对应战斗 BGM 资产（仅 Battle_04.ogg），已记录待补资产。

## 单元 6 · 第一章战斗回归集【完成】

- **四场第一章 Boss 战注入回归**（troop 16/17/20/19，Lv4 队伍）：战斗正常推进（普攻/反击/敌计策回血如赤心计/智复计全正常），累计 20+ 回合**零异常零报错**——今晚战斗层改动（敌群事件页/122 全参数位）未破坏常规战斗；
- **胜利管线确定性验证**（troop 6+注入事件页回合1全灭，`ch1-victory.pre/end.json` 独立完成）：`胜利！`→ 获得经验 48、金 28、掉落 护身烟×2，全员 exp 入账 —— **allOk=true，零异常**（奖励/掉落/升级链路今晚改动后完好）；
- **开场里程碑**：45s autointro 推进至 var1=5（开局+5）。**已知限制**：autointro 只按确认不走路，宫廷内移动需页面驱动器（此前走查已用；文档 00:40 记录走位路径），完整第一章自动走查列为后续单元；
- **节奏观察**（记录不擅动）：回合 ~9s（动画等待为主，属并行会话战斗表现层领域），低等级打 Boss 1 点伤害属解包原值强度。
- 提交：`（下轮一并）`

## 单元 7 · 统一回归框架 + 仓库功能【完成】

### 7a. 统一回归框架（`测试/tools/regress.mjs`）
- 一键顺序执行全部 CDP 用例（formation/inn-core/persist/grow/troop-events/menu-tour/audio-check/ch1-victory/equip-8slot/storage），自动抓取 endEval 断言 + exceptions + console 噪音，输出 PASS/FAIL 汇总，非零失败退出码；
- **首轮全量回归：9/9 全绿，全部零异常零 console 错误**（含昨晚全部改动集成的整体健康证明）。

### 7b. 仓库功能（BrotherJie_MenuBase/CallActorStorage，数据 28 处调用）
- `Game_Party`：`storageCount/storageGain/storageLose/storageAll`（`_storage` 随存档 v2 持久化，additive 字段旧档默认 {}）；
- `Scene_Storage`（menus.js）：背包⇄仓库双栏、左右切换、回车整叠存入/取出、Esc 关闭；`T.openStorage()`；
- 插件 357 导线：interp.js 主解释器 + data.js `runMapCommonEvent` 识别 `BrotherJie.../CallActorStorage` → 打开仓库（物品合成 `CallItemSynthesis` 暂挂起，配方载荷语义无插件源码佐证，已记录）；
- **实测（CDP，`storage.pre/end.json`，零异常）`allOk=true`**：开场景 → 回车整叠存入（蛇胆×2→仓）→ 取出 → 存档往返（bag/sto 原样恢复）。
- 提交：`（见下）`

## 单元 8 · 数值/AI 静态核对 + 商店实测【完成】

- **伤害公式核对**：攻击=引擎 `getAttackPower`（=A线规格 atk×4−def×2）；计策=我方 `coef×mat÷256`、敌方 `coef×mmp÷960` —— 与 A 线规格一致，无需改动；
- **敌 AI 条件分布**（2159 条行动）：常时 1579、HP% 290、**MP% 290** —— 引擎 MP 条件为「恒真简化」（历史取舍），可能导致带 MP 门槛的敌人无脑放计；参数语义未知（MZ type3=MP 低于 X%），**记录为已知差异项，待原版实测后按语义实装**；
- **商店实测**（`shop.pre/end.json`，Scene_Shop 直接实例化+注入回车，零异常）`allOk=true`：蛇胆(1000金)买入 金2500→1500、数量 0→1；余额不足正确拒绝（金0仍无变化）。
- 提交：`（见下）`

## 单元 9 · 标题「继续征程」读档循环验证【完成】

- **端到端持久化回路**（`title-continue-a/b/mid` 三用例，一次运行内 存盘→location 重载无 autostart→标题页→注入「继续」→读档回地图）：`allOk=true`，金77/蛇胆1 原样恢复，全程零异常；
- 回归框架 regress.mjs 支持 mid/midAt（中途重载类用例），title-continue 已并入默认集；
- 提交：`（见下）`

## 单元 10 · 技能菜单施法实测【完成】

- **Scene_Skill 非战斗施法**（`skill-menu.pre/end.json`，注入回车，零异常）`allOk=true`：刘备 Lv10 技能页找到「赤心计」（scope 我方单体）→ 选择并确认 → 兵力 5→630、谋点按技能 mpCost 扣除（mpDelta=mpCost ✓）；
- 附带确认：Lv1 刘备技能菜单为空（杀毒计等 5 级起学），属原版升级习得设计，非缺陷；
- 已并入回归默认集；提交：`（见下）`

## 单元 13 · 第一章自动走查器（页面内 BFS 寻路）【MVP 达成】

- **走查器**（`cases/walker-intro.pre/end.json`）：页面内注入控制器——BFS 寻路（基于引擎 canPass，网格级路径）+ 消息窗自动确认 + 行动按钮互动（面向+确认+侧步）+ 战斗自动推进（每拍确认 + 开战强化队伍加速）+ 多路点队列按到达推进；ROUTES 表按地图可扩展。
- **实测里程碑**（真实行走,零异常）：
  - v2（互动原型）：开场演出 → 宫殿 → 曹操对话 → **收朱灵/路昭**（var1=5、队伍3人）；
  - v4（路点队列）：**徐州收关羽+张飞,var1=25,到达徐州城**——与历史走查第一阶段完全一致；
  - BFS 版：宫殿内**收关羽（var1=15）**可达;宫殿门时序与徐州分支存在剧情变数,完整通关（var1=45）留作后续调优（早晨路线资料已齐:27门→23@95,116→纪灵115,141→袁术156,92→28→168→31→高升30,21→总攻21,6）。
- 提交：`（见下）`

## 单元 14 · 装备换装 UI 冒烟【完成】

- `equip-smoke.pre/end.json`：直接推 `Scene_Equip`，注入 down/up/ok/PgUp/PgDn/cancel 序列驱动场景更新——**零异常、零 console 错误**（列入了回归默认集；装备 8 槽数值属性另有 `equip-8slot` 覆盖）。
- 提交：`（下轮一并）`

## 单元 16 · 队伍编成（Lineup）【完成】

- **缺口**：MAX_BATTLE_MEMBERS=5,53 名可收武将,引擎/数据无任何换人机制——后期武将溢出时第 6+ 位无法上场；
- **实现**（menus.js）：`Scene_Lineup` 双栏（出战 5 人 / 候补）,回车交换、左右切栏、Esc 关闭;主菜单插入「编成」（物品/技能/装备/编成/状态/存档/离开,itemMax 6→7）;顺序即 `_actors` 排序 → 存档天然持久化;
- **实测**（CDP,`lineup.pre/end.json`,零异常）`allOk=true`：六人队（+赵云12+诸葛亮14）→ 出战[0]↔候补[0] 交换 → `_actors`=[14,3,4,5,12,2]、`battleMembers[0]=诸葛亮`（刘备进候补）;save→load 顺序保持;
- 地图自动 BGM（542/552 图）核对：引擎 mapscene 已实现且 18 个 BGM 资产齐全——无缺口,确认即收；
- 提交：`（见下）`

## 单元 21 · 菜单武将翻页 + 存档槽位 UI【完成】

- `menu-nav.pre/end.json`（零异常）`allOk=true`：Scene_Skill PgDn/PgUp 成员切换（0→1→0）;存档 UI 写槽 1（version 2）;
- 已并入回归默认集；提交：`（见下）`

## 单元 22 · 走查器收官冲关（时间盒内）【结论留档】

- 新增「门口扫描」策略（159 门行多次确认+横扫三扇门）,本轮实测停在 收关羽(var1=15) 后**宫殿门未开**（故事门时序需更多节拍:门后剧情可能依赖更多宫内事件,如 张飞/吕布 节拍）;
- 走查器保留为冠军资产:已实测达成 收朱灵/路昭(var1=5) 与 收关羽+张飞至徐州(var1=25) 两里程碑;BFS+互动+战斗推进+多路点队列均可扩展;
- 完整第一章自动通关(var1=45)标记为明早路线考古项目。
- 【最终回归 21/21 全绿】8c31054: lineup 持久化用例显式校验 hasSave6b/loadRet(消除时序 flake),版本 v10;全量复跑含 阵型/客栈/存档往返/成长道具/敌队页/菜单巡览/音频/第一章胜利/仓库/商店/续档/技能菜单/装备/编成/行动顺序/战败复活/计策公式/战斗道具/战斗范围/菜单导航/8槽装备。

## 单元 23 · 军物品合成系统 + 主解释器 357 修复（05:00-05:30）

- **军物品合成（役店）**：36 张役店地图、17 个唯一配方、76 次插件调用考古完成。
  配方载荷语义：`BrotherJie_ItemSynthesis/AddRecipe` 参数 `{recipe:"[...]"}` = [材料0-53 子集, 产物54-63]；
  `CallItemSynthesis` 打开合成界面。产物实体：54 九转丹 / 55 宝马 / 56 降龙罩（57-63 为无名占位）。
- **实现**：data.js `registerSynthRecipe`（解析配方→材料/产物，跨 655 续行兼容）+ `$gameParty.canSynth/synth`
  （持全部材料各≥1→各-1→得产物）；menus.js `Scene_Synthesis` 双窗（合成品类+所需材料 ✓/✗）；
  data.js `runMapCommonEvent` 与 interp.js 主解释器 357 均接入 AddRecipe/CallItemSynthesis。
- **暴露并修复深埋 bug**：interp.js `command(cmd)` 内 `const c=cmd.code` 为数字，旧代码 `c.parameters`
  恒 undefined → 主解释器 357 插件命令（含仓库 CallActorStorage！）在地图事件链中从未生效。
  旧 storage 用例直接调 `T.openStorage()` 故未暴露。改用 `p`，三通路 + 真实 map157 EV004 事件链用例全绿。
- 用例：synth-shop（界面/消耗/产物）、synth-e2e（真实 map157 事件列表→配方注册）、
  synth-interp（主解释器 357 三通路）→ 全部 allOk。
- **lineup 持久化用例竞态根因（全量回归偶发 FAIL）**：end eval 手动 `sc.update()` 完成编成换人后，
  残留 `T.Input._down={ok:true}` 被**主循环**下一帧的 Scene_Lineup.update 再次消费 → 触发第二次换人
  把顺序换回 → loadGame 后 restored0=2(默认序首元素)。修复：swap 断言后清空 `_down/_prev`，5 连跑全绿；
  也确认引擎本身无 bug（写盘 JSON、loadGame 恢复均正确）。
- **回归竞态两连修（input 残留 vs goto 钩子时序）**：lineup 是 end eval 残留 `_down={ok}` 被主循环
  二次消费；inn-core 是 pre 在 goto 钩子(resetGameState)落地前设数据被重置。均单跑连绿+全量随动。
- **最终全量回归 24/24 全绿**（含 formation/客栈/存档往返/成长/敌队/菜单/音频/第一章胜利/仓库/商店/
  续档/技能/装备/编成/战斗6例/合成3例/8槽装备），零异常零 console 错误。
- **单元 24 · 敌方行动比例条件语义修复 + 指令覆盖审计（约 06:00-06:20）**
  - 审计：地图+公共事件 62252 条指令、65 个去重码，引擎 command() 覆盖 64 码；唯一"缺失"的 657
    是 BrotherJie AddRecipe 的配方副本行（静默忽略，无害）——指令层无缺口。
  - 修复：敌行动条件 type2(290条, HP≤0.5 低血回复计)/type3(290条, MP≤0.2 回蓝计)此前完全忽略
    param2（param1 恒 0），导致敌人满血乱放回复、回蓝计永远可用；现 param2>0 优先作比例阈值，
    param1=0 且无 param2 回退恒真兼容旧行为。action-cond 用例锁定五断言，25/25 全绿。
- **单元 25 · 地图步数解除状态 + 逃跑守卫 + 全量 26/26（约 06:20-06:50）**
  - 审计：数据 98 条状态带 stepsToRemove(烟遁256/杀毒128/灼伤中毒等100)，引擎完全未消费；
    现 `Game_Battler.onMapStep` 消费 `_stateSteps` 计数，`$gameMap.onPlayerStep` 每步推进，
    addStateRaw/removeStateRaw 登记/清理。step-state 用例：100 步保留、256 步精确解除、真实行走接线。
  - effect 41(逃跑技能,数据技能3)补守卫：actor 触发纳入 tryEscape。
  - 指令码审计收官：数据 65 码全覆盖(657=AddRecipe 配方副本行静默忽略)；effects 8 码全覆盖(含 41/42/44)。
  - inn-core 改为轮询等待客栈链(扣款+治愈)完成，消除固定时长读盘 flake。
  - 【最终全量回归 26/26 全绿】。
- **单元 26 · 行动结束状态自动解除 + 脚本API审计 + 走查器突破（约 06:50-07:20）**
  - 修复：autoRemovalTiming=2(行动结束)状态的回合自动解除此前完全缺失（数据 20 个：
    灼伤/冰冻/中毒/重伤/晕眩/嘲骂/烟遁/击返/策返/疑心/离间/缚杀/倍击/免击/免策/佳人/艳行等），
    只能战末 removeAtBattleEnd 清理；现与 timing=1 同样按 min/max 回合计数推进。state-timing 用例 3 连绿。
  - 脚本 API 审计：数据全部 355 脚本调用（storageRemoveActor/changeLevel/allMembers/actor/hasItem/loseItem）
    引擎全覆盖。opcode 审计最终：地图+公共事件 62252 条/65 码全覆盖（657=AddRecipe 配方副本行静默忽略）。
  - 走查器突破：宫殿门 stuck 分支方向修正（门在下方却向上走）→ 实测从宫殿经门传送到徐州城，
    4 人队（刘备/关羽/张飞/朱灵）大地图自由移动；路线升级为里程碑链（纪灵/袁术/袁胤战+战斗等级递增）。
    door-e2e 用例锁定门传送链路（引擎本身无 bug）。var1=45 全流程冲关留明早考古。
- **单元 27 · 敌掉落概率语义修正（约 07:00-07:25）**
  - 重大 bug：MZ 语义 denominator 为「1/N 概率」（denominator=1=必掉），引擎误用 `rand(100)<denominator`
    当百分比 → 数据 768 条必掉掉落只按 1% 概率，玩家几乎打不出任何击杀掉落（包含关键剧情物品）。
    现改为 `rand(max(1,denominator))===0`。drop-rate 统计用例：denominator=1 必掉(2000/2000)、
    =20 稀有≈5%(114/2000) 三连绿。
  - 命中/闪避/暴击/状态率公式审计：均 MZ 语义无同类问题。
  - 【最终全量回归 28/28 全绿】。
- **单元 28 · 敌方特性三连补（约 07:25-07:50）**
  - specialFlag(2)=必定先攻：MZ 语义，数据仅典韦×4/周泰；此前未消费。makeSpeed 吊顶 +10000 保证先手。
  - 敌方多动：ATTACK_TIMES(34, 陈兰/李蒙/宋宪/张允×3/黄忠/李湛×2/候选/李严×2/曹休/高览/满宠/曹植/
    韩当/丁奉/李广/泥弓 22 个×2 动) 与 ACTION_PLUS(61, 秦始皇/王皇帝/田丰/秘将军/幻钟王 追加行动)
    经 buildTurnOrder 重复插入（此前回合只行动一次）。
  - 用例：fast-attack（先手吊顶/无flag正常顺序）、multi-actions（×2敌人两次/单次敌人一次/我方一次）。
  - 【最终全量回归 30/30 全绿】。
## 收尾 · 全量回归与总结

- `battle-scope.pre/end.json`（troop15,零异常）`allOk=true`：
  - **全体敌方伤害**（雷玉,scope2）：applyItem 一次命中全部 5 名存活敌人（伤害 1/只为 Lv1 低值,引擎公式 getAttackPower×0.2）；
  - **我方单体恢复**（赤心计,scope7,效果800×mat/256）：低血成员一次回满（594 公式值,封顶 mhp 241）——恢复封顶语义正确；
- 已并入回归默认集；提交：`（见下）`

## 单元 19 · buff 生命周期 + 计策伤害公式实测【完成】

- `tactic-formula.pre/end.json`（troop15,零异常）`allOk=true`：
  - **buff 回合生命周期**：1 回合增益 → `updateBuffsTurns` 后归零；
  - **水星计伤害管线**：`680×mat/256 × 元素率 ±10% 方差` → 实测 531(期望)→568(命中区间 [478,584],元素抵抗参与)；
- `battle-item.pre/end.json`（战斗内道具冒烟）`allOk=true`：道具指令 → select-item 列表含赤心丹，零异常；施放完成环节标记「待真人确认」（战斗重构期 UI 时序敏感）;
- 提交：`（见下）`

## 单元 18 · 逃跑指令可见性 + 战败恢复链路【完成】

- `defeat-recover.pre/end.json`（troop15,零异常）`allOk=true`：
  - `canEscape=true` 指令含「逃跑」,置 false 后指令隐藏（Boss 战无逃跑按钮）；
  - 全队倒下 → phase defeat → `finish(canLose)` → **全员复活 hp≥1 回地图**（战败不 game over 的剧情链路）；
- 敌人施法扣 MP 核查：`e.mp -= min(e.mp, mpCost)` ✓（无无限 MP 隐患）;
- 已并入回归默认集;提交：`（见下）`

## 单元 17 · 战斗行动顺序与数值管线【完成】

- `battle-order.pre/end.json`（troop6 战斗,零异常）`allOk=true`：
  - 行动顺序按 `turnAddSpeed` 降序（刘备219→山贼35,排序稳定）；
  - 会心率 `critChance` 钳制于 [0,0.5]（实测 0.03）；
  - **雁行阵 agi×1.8（185→333）参与初始化计算**——阵型速度加成在战斗排序中生效；
- 已并入回归默认集；提交：`（见下）`

## 单元 15 · 收官全量回归【完成】

- **最终全量回归（14 用例）**：`formation / inn-core / persist / grow / troop-events / menu-tour / audio-check / ch1-victory / storage / shop / title-continue / skill-menu / equip-8slot / equip-smoke` —— **14/14 全绿、exit 0、全部零异常零 console 错误**；
- 通宵累计：**18 笔提交**入权威仓库（见 git log），A 线 327+78 项断言保持全过；
- 踢馆待续（明早可接）：走查器补完宫殿门时序/徐州分支→var1=45；合成系统配方语义；敌 MP 条件；阵型/客栈定价实机比对。

- **战斗层全量入账**：`b776ded` 一次提交收录 并行会话战斗UI工作（总攻自动连击/K停止、头像卡、敌情标签页、受击反馈、目标记忆）+ 本会话战斗层补全（敌群事件页、效果码42战侧、122全参数位、战斗多行脚本）——battle.js/index.html 连夜累积的未提交状态已清零；
- **regress 健壮化**：每用例前清理残留 9333 端口（cdp-driver 自带 pkill 失效的补丁）+ 单用例超时上限 400s；title-continue 独立复跑确认正常退出；
- **全量回归重跑（13 用例集）**：**12/13 通过、exit 0**；唯一 FAIL 为 menu-tour 套件内偶发（endEval 未产出），单跑复现 `allOk=true`、零异常——实为全绿；
- **A 线跨线核对**：`three_kingdoms_test.js` 327 项 + `three_kingdoms_smoke.js` 78 项 全部通过。
- A 线跨线核对：`three_kingdoms_test.js` 327 项 + `three_kingdoms_smoke.js` 78 项 **全部通过**。

> 注：battle.js 本轮改动与并行会话未提交的战斗 UI 工作同文件共存，未单独提交（遵循既有留待策略）。