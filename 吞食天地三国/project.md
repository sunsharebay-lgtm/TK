# 吞食天地三国 · project.md

## 定位

创意空间子项目：吞食天地Ⅱ同人网页复刻。

## 交付规格（开发线 v0.2.0）

- 主入口：`网页复刻/吞食天地Ⅱ同人复刻-网页版/`。
- 已实现：59 职业、145 策略、273 武器、146 防具、402 敌人、256 敌群；完整战斗公式；第一章「灭袁术」流程；地图/菜单/存档体系。
- 最近完成：主菜单/商店 tab、兵力条固定参考系、战斗体验修复、第一章战斗结果闭环、世界地图遇敌、敌方还击、装备库存、物品使用/丢弃、商店出售、存档槽位校正、武力/兵力攻击成长、第一章出口收尾、郑家村提示、新武将等级同步、阵亡武将复活目标、全章节阶段索引、公共剧情道具链、后续章节地图事件接入和章节试玩中心、载具航行与剧情渡船；合成系统暂缓处理。
- 回归：`测试/three_kingdoms_test.js`、`测试/three_kingdoms_smoke.js`、`测试/tools/regress.mjs`；新增 `preview-center` 覆盖章节节点状态恢复和调试存档隔离，`item-target`、`boat-route`、`title-return` 覆盖本轮修复。

## 处理决策

- 网页复刻为主推进线，旧的 FC 单文件版保留为历史。
- 数据与素材来自原版参考，只在本项目内维护。
- 稳定切片用 `game/three-kingdoms/vX.Y.Z` 标签发布。

## QA 结果（最近验证）

- 三国回归与冒烟测试通过；最近一次全量浏览器回归 62/62，单元测试 327 项、冒烟测试 78 项；覆盖第一章胜利/终局出口、客栈、编成、BGM 回退、商店购买/出售、物品使用/丢弃、存档槽位、菜单联动、战斗兵力条、敌方还击、敌方难度、世界遇敌、宝箱、训练场、章节入口、郑家村书信路线和陆地坐标、新武将平均等级、阵亡武将复活目标、恢复技能目标选择、重复装备库存、武力/谋略规则、后续章节公共道具链、章节边界、伤害浮字、章节试玩节点、恢复道具目标、渡船、返回标题、地图道具和群体技能目标。

## 当前决策

- 阵型真实倍率采用当前推算值，不等待原版实机校准。
- 客栈价格采用 `城镇档位 × 4` 金。
- BGM 缺失时回退到 `Battle_04`。
- 战斗经验在原始敌群经验基础上按 `1.25` 倍结算；敌方不再有不消耗资源的被动兵力/谋略回复。
- 敌方策略先检查谋略点；普通回复按固定基础值与智力倍率结算，完复计直接补满；策略概率和策略效果均受智力影响。
- 敌方恢复计只作用于施法者自身，谋略点耗尽后不会继续使用恢复策略。
- 物理切换武将按键为 `U/I`。
- 武将武力使用历史基准值，最终武力为基准加装备；物理攻击随当前兵力规模提升，谋略消耗按原始值约三分之二计算，谋略上限随等级额外成长。
- 第一章袁术最终战后写入 `chapter1Complete`，山洞上方出口可返回大地图；阶段变量从 80 起按章节索引进入后续主线，二周目秦皇陵使用独立阶段区间。
- 存档槽位显示与实际槽位 0-3 一一对应；物品页可管理全部背包物品，商店可按 `Q/E` 切换出售/购买，出售按原价 75% 回收。
- 郑家村补充村民提示；地图 170「郑玄居」保留原始 stage 65 书信事件，推进到该节点后可取得郑玄的信。
- 新收武将通过 `Game_Party.addActor` 同步当前队伍平均等级并满兵力/谋略；物品页的招魂丹（scope 9）只列阵亡出战武将供选择。
- 非战斗恢复技能按原始 scope 分流：scope 7 单体我方先选目标，scope 9/10 只作用于阵亡武将；选中技能与确认目标分属两个输入帧，避免误把刘备当成默认目标。
- 郑玄居在网页版默认视为已完成原作登录插件，读档也会迁移开关 55；事件条件 `[8,物品ID]` 表示拥有物品，郑玄的信可在有地图时把剧情变量推进到 70。
- 后续章节沿原始地图事件数据推进，阶段范围与地点见 `文档/全章节主线规划.md`；目前已验证第二章入口和跨章节关键道具，后续试玩继续按章节节点补充实机回归。
- 合成打造、天赋称号、PVP 暂不作为当前版本功能。
- 试玩中心仅在 URL 带 `debug=1` 或 `preview=` 时启用；`F6` 的现场快照和调试存档与正常存档隔离。敌方策略最高触发率由 42% 下调至 36%，用于降低连续计策造成的前期压力，同时保留智力对概率和效果的影响。
- 赤心丹等非战斗恢复道具的目标列表正确设置成员数量，可用上下键选择其他队员；补齐标准载具状态、船只通行标志、登船/下船和载具位置存档；主菜单“返回标题”回到标题画面。
- 载具在大地图上只允许驶入海水图块，船体位置跟随玩家同步；剧情自动航行期间屏蔽遇敌，结束下船会落到最近的合法陆地。自由航行时使用海贼敌群，不会把陆地敌群带到海上。

## 当前源码校验

- `网页复刻/吞食天地Ⅱ同人复刻-网页版/index.html`：`2a8e17a786d3c11df1cec9a9708db215ad42ad9373a280cc5ca9c2a45f4fb335`
- `网页复刻/吞食天地Ⅱ同人复刻-网页版/js/battle.js`：`31b4c2eabf34a042c67738387ff44d1f7d138fe4b72b77178e80293c80a0f354`
- `网页复刻/吞食天地Ⅱ同人复刻-网页版/js/data.js`：`00cd8ac6351782b54466d6f325c8e69c577a5780f6337e204428a25a22b18e4c`
- `网页复刻/吞食天地Ⅱ同人复刻-网页版/js/interp.js`：`d27c1ace4bf09b4029bc33c963520edc12f3912707cab6044d22d3606c14c1ab`
- `网页复刻/吞食天地Ⅱ同人复刻-网页版/js/menus.js`：`4c5a3afe8e194e251d4587566c6d34bd6243edf997b14a91f3865c80ab0f8a44`
- `网页复刻/吞食天地Ⅱ同人复刻-网页版/js/mapscene.js`：`18ade9fe3c2d5b390316e8dc89dd42671629aae6d73afae840e430dcc2319085`
- `网页复刻/吞食天地Ⅱ同人复刻-网页版/js/core.js`：`ba5d644c582997f74f0884bfaae6d6af1baccf7619dbd449f15cbf3fcd42a4cb`
- `网页复刻/吞食天地Ⅱ同人复刻-网页版/js/preview.js`：`6ef2e364b3f63d3272facd7d400a18148ce3c2b794976153cb573377aca9c32c`
