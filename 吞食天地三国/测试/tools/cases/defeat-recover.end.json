["(async()=>{const bs=T.BattleScene;const out={};const fails=[];const CHK=(k,v)=>{out[k]=v;if(v!==true)fails.push(k+':'+JSON.stringify(v));};\nif(!bs)return JSON.stringify({noBattle:true,allOk:false,fails:['no-battle']});\n// 1) canEscape=true → 指令含「逃跑」；再验 canEscape=false 时隐藏\nout.hasEsc=bs.visibleCmds(bs.partyMembers[0]).some(c=>c[0]==='逃跑');CHK('escVisible',out.hasEsc);\nbs.req.canEscape=false;out.hidden=!bs.visibleCmds(bs.partyMembers[0]).some(c=>c[0]==='逃跑');CHK('escHidden',out.hidden);bs.req.canEscape=true;\n// 2) 战败恢复: 全队倒下 → defeat → finish(canLose) 全体复活 hp1\nfor(const a of bs.partyMembers){a.hp=0;a.addStateRaw(1);}\nbs.phase='anim';bs.afterAction();\nout.afterPhase=bs.phase;CHK('defeatTrigger',bs.phase==='defeat');\nbs.phaseTimer=2;\nawait new Promise(r=>setTimeout(r,500));\nconst allDead=$gameParty.battleMembers().every(a=>a.isDead());\nout.allDeadAfter=allDead;CHK('revived',!allDead&&$gameParty.battleMembers().every(a=>a.hp>=1));\nout.allOk=fails.length===0;out.fails=fails;\nreturn JSON.stringify(out);})()"]