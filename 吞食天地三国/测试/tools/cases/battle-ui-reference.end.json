["(()=>{const s=T.BattleScene;const enemy=s.troop.members.map(e=>Math.round(s.enemyBarRate(e)*100));const party=s.partyMembers.map(a=>Math.round(s.partyBarRate(a)*100));return JSON.stringify({enemy,party,enemyRef:s.enemyBarRefHp,partyRef:s.partyBarRefHp,allOk:enemy.includes(100)&&party.includes(100)});})()"]
