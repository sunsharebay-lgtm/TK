["(()=>{if(!T.$gameMap||T.$gameMap.mapId!==23)return 'false:map';T.$gameParty._actors=[2,3,4,5,6];const a=T.getActor(2);for(const x of T.$gameParty.allMembers()){x.changeLevel(10,false);x.recoverAll();}const b=new T.Scene_Battle({troopId:11,canEscape:true},T.$gameMap.interpreter);const allies=b.partyMembers.slice(0,3);for(const x of allies)x.hp=1;const heal=T.$dataSkills[35];a._pendingAction={kind:'skill',skill:heal};b.currentActor=a;b.selectMode='target-ally';b.targets=allies;const healToggle=b.toggleAllTargets();const originalRandom=Math.random;Math.random=()=>0.5;b.applyItem(a,heal,a._pendingAction.targets);const totalHeal=allies.reduce((sum,x)=>sum+x.hp-1,0);for(const x of allies)x.hp=1;b.applyItem(a,heal,allies[0]);const singleHeal=allies[0].hp-1;const enemies=b.troop.aliveMembers().slice(0,3);for(const x of enemies)x.hp=x.mhp;const fire=T.$dataSkills[11];a._pendingAction={kind:'skill',skill:fire};b.selectMode='target-enemy';b.targets=enemies;const damageToggle=b.toggleAllTargets();b.applyItem(a,fire,a._pendingAction.targets);const totalDamage=enemies.reduce((sum,x)=>sum+x.result.hpDamage,0);for(const x of enemies)x.hp=x.mhp;b.applyItem(a,fire,enemies[0]);const singleDamage=enemies[0].result.hpDamage;Math.random=originalRandom;window.__aoe={healToggle,damageToggle,totalHeal,singleHeal,totalDamage,singleDamage};return 'ready';})()"]
