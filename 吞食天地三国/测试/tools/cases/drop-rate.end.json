["(async()=>{const out={};const fails=[];const CHK=(k,v)=>{out[k]=v;if(v!==true)fails.push(k+':'+JSON.stringify(v));};\nconst t0=Date.now();while(Date.now()-t0<4000&&(!T.$gameMap||!T.$gameMap.interpreter)){await new Promise(r=>setTimeout(r,50));}\nconst proto=Game_Enemy.prototype;\nconst probe=Object.create(proto);\nprobe.dropItems=[{kind:1,dataId:54,denominator:1},{kind:1,dataId:55,denominator:20}];\nconst N=2000;let always=0,rare=0;\nfor(let i=0;i<N;i++){const ds=probe.makeDropItems();if(ds.some(x=>x&&x.id===54))always++;if(ds.some(x=>x&&x.id===55))rare++;}\nout.always=always;out.rare=rare;\nCHK('denom1Always',always===N);\nCHK('denom20Rare',rare>0&&rare<N*0.15);\nout.allOk=fails.length===0;out.fails=fails;\nreturn JSON.stringify(out);})()"]
