# 历史版本归档

`超级像素兄弟` 项目确认版本的旧源码归档在这个目录。

## 命名规则

- 成品文件：`index.html`（由 `src/` + `build.sh` 生成）
- 归档文件：`超级像素兄弟-vX.Y.Z.html`（版本号与 `更新记录.md` 中的条目一致）

## 归档时机

- 只有经过用户确认的版本才算新版本。
- 确认新版本时：把即将被替换的上一版 `index.html` 复制归档到这里，再在 `更新记录.md` 末尾追加记录。
